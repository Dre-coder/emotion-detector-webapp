services:
  - type: web
    name: emotion-detector-webapp
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir emotion_detector --bind 0.0.0.0:$PORT app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.6
      - key: MODEL_MODE
        value: deepface
      - key: FLASK_ENV
        value: production